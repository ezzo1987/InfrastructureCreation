---
- name: Exercise for setting up a web server in an EC 
  # hosts: ubuntu
  # Use the public IP if the VM is already created
  hosts: 34.204.42.8
  user: ubuntu
  become: true
  become_method: sudo
  become_user: root
  roles:
   - setup